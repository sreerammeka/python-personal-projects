# Arithmetic Operators
+
-
/
*
%


# Assignment Operators
x = x + 2 can be written as x + = 2
x = x * 3 can be written as x * = 3

#Assign values in one line
x, y = 2, 7 assigns x = 2 and y = 7

#Assign negative values using unary operator
x =-x will assign a negative value of x and assign it back to x

#Logical Operators
#Comparison
a < b will return True or False
a > b will return True or False
a == b compares if two values a and b are same, will return True or False

#Relational operators
a <= b will return True or False
a >= b will return True or False
a !=b will return True or False

#Logical operators (and, or, not)
#and
a < 5 and b > 6 will return True or False
#or
a < 5 or b > 6 will return True or False
#not
not a will negate the value, return True if previous value is False or return False if previous value is True

#Bitwise Operators (~, bitwise and (&), bitwise or (|), bitwise xor (^), left shift (<<), right shift (>>))
#Complement (~)
#Complement reverses zeros and ones in binary format
#Negative numbers are stored using Complement
~0 results in 1
~1 results in 0
~12 results in -13, so to store -13 wee can store it as ~12



